// std/math.intent — 数学运算模块
// 基础运算

def add(a: Int, b: Int) -> Int {
    return a + b;
}

def multiply(a: Int, b: Int) -> Int {
    return a * b;
}

def square(x: Int) -> Int {
    return x * x;
}

def abs(x: Int) -> Int {
    if x < 0 {
        return 0 - x;
    }
    return x;
}

def max(a: Int, b: Int) -> Int {
    if a > b { return a; }
    return b;
}

def min(a: Int, b: Int) -> Int {
    if a < b { return a; }
    return b;
}
