def count_to(n: Int) {
    let mut i = 0;
    while i < n {
        yield i;
        i = i + 1;
    }
}

let nums = count_to(5);
print("generator result: " + str(nums));
print("len: " + str(len(nums)));