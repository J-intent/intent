import std.bim.geometry;
import std.bim.pipe;

// 测试 PipeSegment 类是否可见
let seg = PipeSegment();
seg.create(Point3D(), Point3D(), 200.0, 10.0);
print("PipeSegment OK");

// 测试 PipeOffset 类是否可见
let off = PipeOffset();
print("PipeOffset OK");